<%= form_with(model: department) do |form| %>
  <% if department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
        <% department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col sm-8 mb-3">
    <div class="actions">
      <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <%= form.submit class:"btn btn-success text-white me-md-2" %>
        <%= link_to 'Go Back', departments_path, class:"btn btn-outline-primary text-primary" %>
      </div>
    </div>
  </div>

  <div class="form-floating mb-3">
    <%= form.text_field :description, class:"form-control", id:"floatingInputDepartment", placeholder:"Finance" %>
    <%= form.label :description, for:"floatingInputDepartment"  %>
  </div>

    <div class="form-floating mb-3">
    <%= form.select :floor_id, options_for_select(Floor.order(:description).pluck(:description, :id)), {}, {class: "form-select "} %>
  </div>
<% end %>
